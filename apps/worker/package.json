{
  "name": "@xpose/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "check-types": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@xpose/protocol": "workspace:*",
    "hono": "^4.7.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.12",
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "5.9.2",
    "vitest": "^3.2.0",
    "wrangler": "^4.14.0"
  }
}
